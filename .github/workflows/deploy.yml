name: Github actions 실행하기

on:
  push:
    branches:
      - main

jobs:
  Manual-Deployment-Job:
    runs-on: ubuntu-latest

    steps:
      - name: applicaton.yml 생성
        run: |
        pwd
        ls ./src/main/
        echo ${{secrets.APPLICATION_YML}} > ./src/main/resources/applicaion.yml